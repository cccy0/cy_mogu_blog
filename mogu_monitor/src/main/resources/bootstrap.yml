server:
  port: 8606

spring:
  application:
    name: mogu-monitor
  cloud:
    nacos:
      username: nacos
      password: Lili0123
      discovery:
        server-addr: nacos:8849
        namespace: prod
      config:
        server-addr: nacos:8849
        file-extension: yaml
        #指定分组
        group: prod
        #指定命名空间
        namespace: prod
